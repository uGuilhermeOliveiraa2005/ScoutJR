[build]
  command = "npm run build"
  publish = ".next"

# O Netlify detecta automaticamente o Next.js e instala o plugin necessário (Next.js Runtime),
# mas você pode forçar configurações específicas aqui se necessário no futuro.

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(), interest-cohort=()"
